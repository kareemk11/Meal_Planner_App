<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MealPlanner"
        tools:targetApi="31">
        <activity
            android:name=".LocalMeal.View.LocalMealActivity"
            android:exported="false" />
        <activity
            android:name=".Meal.View.MealActivity"
            android:exported="false" />
        <activity
            android:name=".ListedMeals.View.ListedMealsActivity"
            android:exported="false" />
        <activity
            android:name=".SplashActivity.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeActivity.View.HomeActivity"
            android:windowSoftInputMode="adjustPan|stateHidden"
            android:exported="false" />
        <activity
            android:name=".Authentication.Registeration.RegisterView.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".Authentication.Login.LoginView.LoginActivity"
            android:exported="false" />
    </application>

</manifest>